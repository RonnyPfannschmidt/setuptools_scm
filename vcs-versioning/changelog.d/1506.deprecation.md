`scm.git.pre_parse` is deprecated in favour of the `on.*` settings. Existing
values keep working and are mapped onto them, including the fact that
`fail_on_missing_submodules` used to replace the default shallow warning
rather than add to it. The `pre_parse` callable argument to the git parse
function is unaffected.
